services:
  - type: web
    name: mvp-server
    runtime: node
    plan: free
    region: frankfurt
    rootDir: .
    buildCommand: |
      npm install -g pnpm@9.15.0
      pnpm install --ignore-scripts --no-prod
      pnpm --filter @mvp/server build
    startCommand: pnpm --filter @mvp/server start
    healthCheckPath: /health
    autoDeploy: true
    envVars:
      - key: NODE_ENV
        value: production
      - key: CORS_ORIGIN
        value: "*"
      - key: PORT
        value: "10000"
